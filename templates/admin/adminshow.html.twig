{% extends 'base.html.twig' %}

{% block title %}Admin User show{% endblock %}

{% block body %}
    
<div class="container">
<h1> Admin User Show</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ user.adresse }}</td>
            </tr>
            {#<tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>#}
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn btn-sm btn-info"href="{{ path('admin_index') }}">back to list</a>

    <a  class="btn btn-sm btn-primary" href="{{ path('adminuser_edit', {'id': user.id}) }}">edit</a>
<a  class="btn btn-sm btn-primary" href="{{ path('reloadpassword', {'id': user.id}) }}">modifie password</a>
    {{ include('admin/admin_delete_form.html.twig') }}
    </div>
{% endblock %}
